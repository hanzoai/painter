# Core dependencies for ComfyUI watermark removal workflow
# Install with: pip install -r requirements.txt

# PyTorch (adjust for your CUDA version)
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# ComfyUI dependencies (usually installed via ComfyUI/requirements.txt)
Pillow>=9.5.0
numpy>=1.23.0
opencv-python>=4.8.0
scipy>=1.10.0
einops>=0.6.1
transformers>=4.30.0
safetensors>=0.3.1
accelerate>=0.20.3
huggingface-hub>=0.16.4

# Video processing
imageio>=2.31.1
imageio-ffmpeg>=0.4.8
av>=10.0.0

# SAM2 dependencies (optional)
segment-anything-2>=0.1.0

# Additional utilities
tqdm>=4.65.0
requests>=2.31.0
psutil>=5.9.5

# MLX for Apple Silicon acceleration (optional, Mac M1/M2/M3/M4 only)
# Install with: make install-mlx
# Performance: 70% faster model loading, 35% faster inference
mlx>=0.29.0
mlx-metal>=0.29.0
argmaxtools>=0.1.13
jaxtyping>=0.3.0
coremltools>=8.1
